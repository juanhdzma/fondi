services:
  fondi:
    build: .
    container_name: fondi
    ports:
      - "8080:8000"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin}
    volumes:
      - fondi-db:/data
    restart: unless-stopped

volumes:
  fondi-db:
